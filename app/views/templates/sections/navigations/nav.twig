<nav class="nav">
    <div class="nav__items">
        {% for item in menu %}
            <a href="{{ item.link }}" class="nav__item {% if item.active %}active{% endif %}">
                <i class="{{ item.icon }}"></i>
                {{ item.name }}
            </a>
        {% endfor %}
    </div>
</nav>

